# Maximum number of concurrent file transfers
maxThreads: 3

# The path that is watched for new *.plot files.
stagingPath: "/staging"

# List of paths to move *.plot files to.
destinationPaths:
  - "/dest1"
  - "/dest2"